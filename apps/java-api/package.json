{
  "name": "java-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "./mvnw -B -ntp clean package -Dmaven.test.skip",
    "clean": "rm -rf target",
    "dev": "mvn spring-boot:run",
    "start": "java -jar target/demo*.jar"
  }
}